# This user must have admin rights to this database,
# but should NOT be the root user.
#
# GRANT ALL ON <db_name>.* TO <db_user>@localhost IDENTIFIED BY '<db_password>';
# FLUSH PRIVILEGES

db_user="username"
db_password="pw"
db_name="dbname"
